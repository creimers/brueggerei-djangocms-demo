server {
    listen 80;
    server_name _;

    location /static/ {
        alias /data/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /data/media/;
        expires 7d;
        add_header Cache-Control "public";
    }
}
